{% extends 'base.html.twig' %}

{% block body %}
    <h1>Listado Cmsstzeftypesaccesses</h1>

    <table class="table" id="CMSstzef_list">
        <thead>
            <tr>
                <th>Name</th>
                <th>Id</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for cmsStzefTypesAccess in cmsStzefTypesAccesses %}
            <tr>
                <td><a href="{{ path('admstzef_types_access_show', { 'id': cmsStzefTypesAccess.id }) }}">{{ cmsStzefTypesAccess.name }}</a></td>
                <td>{{ cmsStzefTypesAccess.id }}</td>
                <td>
                    <ul class="list-unstyled">
                        <li>
                            <a class="btn btn-" role="button" href="{{ path('admstzef_types_access_show', { 'id': cmsStzefTypesAccess.id }) }}">show</a>
                        </li>
                        <li>
                            <a class="btn btn-" role="button" href="{{ path('admstzef_types_access_edit', { 'id': cmsStzefTypesAccess.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul class="list-unstyled">
        <li>
            <a class="btn btn-success" role="button" href="{{ path('admstzef_types_access_new') }}">Nuevo cmsStzefTypesAccess</a>
        </li>
    </ul>
{% endblock %}
