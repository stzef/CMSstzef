 
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cmsstzeftypespage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ cmsStzefTypesPage.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ cmsStzefTypesPage.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="list-unstyled" >
        <li>
            <a class="btn btn-primary" role="button" href="{{ path('admstzef_types_pages_index') }}">Listado</a>
        </li>
        <li>
            <a class="btn btn-info" role="button" href="{{ path('admstzef_types_pages_edit', { 'id': cmsStzefTypesPage.id }) }}">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" class="btn btn-danger" value="Borrar">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
