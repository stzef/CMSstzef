{% extends 'base.html.twig' %}

{% block body %}
    <h1>Listado Cmsstzefstates</h1>

    <table class="table" id="CMSstzef_list">
        <thead>
            <tr>
                <th>Name</th>
                <th>Id</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for cmsStzefState in cmsStzefStates %}
            <tr>
                <td><a href="{{ path('admstzef_states_show', { 'id': cmsStzefState.id }) }}">{{ cmsStzefState.name }}</a></td>
                <td>{{ cmsStzefState.id }}</td>
                <td>
                    <ul class="list-unstyled">
                        <li>
                            <a class="btn btn-" role="button" href="{{ path('admstzef_states_show', { 'id': cmsStzefState.id }) }}">show</a>
                        </li>
                        <li>
                            <a class="btn btn-" role="button" href="{{ path('admstzef_states_edit', { 'id': cmsStzefState.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul class="list-unstyled">
        <li>
            <a class="btn btn-success" role="button" href="{{ path('admstzef_states_new') }}">Nuevo cmsStzefState</a>
        </li>
    </ul>
{% endblock %}
