 
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cmsstzefsectionstheme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Selectorcss</th>
                <td>{{ cmsStzefSectionsTheme.selectorCss }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ cmsStzefSectionsTheme.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="list-unstyled" >
        <li>
            <a class="btn btn-primary" role="button" href="{{ path('admstzef_sections_theme_index') }}">Listado</a>
        </li>
        <li>
            <a class="btn btn-info" role="button" href="{{ path('admstzef_sections_theme_edit', { 'id': cmsStzefSectionsTheme.id }) }}">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" class="btn btn-danger" value="Borrar">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
